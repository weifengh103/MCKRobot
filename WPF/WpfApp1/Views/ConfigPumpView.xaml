<UserControl x:Class="WHPG2.Views.ConfigPumpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WHPG2.Views"
             xmlns:vms ="clr-namespace:WHPG2.ViewModels"
             d:DataContext="{d:DesignInstance Type=vms:ConfigPumpViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox Text="{Binding Name}" Margin="317,46,338,356"/>
        <TextBox Text="{Binding Id}"  Margin="328,191,328,211"/>
        <Button Command="{Binding AddPumpCommand}" Margin="270,390,268,10"/>

    </Grid>
</UserControl>
