<UserControl x:Class="WHPG.Views.SenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WHPG.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Height="450" VerticalAlignment="Top">

        <Button Content="Add Pump" Command="{Binding AddPumpCommand}" 
        HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="277,0,0,50" />
        <Button Content="Go to View B" Command="{Binding SwitchToViewBCommand}" 
            HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="400,0,0,50" />
        <!--<TextBox DataContext="{Binding Na}" HorizontalAlignment="Left" Margin="280,155,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>-->
        <!--<TextBox DataContext="{Binding PumpID}" HorizontalAlignment="Left" Margin="277,207,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>-->
    </Grid>
</UserControl>
